# Recitation Activity 1

dic = {'up': 'down', 'down': 'up', 'left': 'right', 'right': 'left', '1':'2'}
x = ("1 UP 2 down left right forward")

dic2 = {'a':'b', 'candy':'three cookies'}
y = ("We are in a house of CANDY")


def translate(translation, msg):
    for x in msg:
        lower = msg.lower().split()
    
        c = translation.pop(f)

    return c
    
print(translate(dic, x))